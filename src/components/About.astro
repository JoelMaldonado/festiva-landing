---
const items = [
  {
    text: "Festiva es la app que centraliza la vida nocturna en Oslo. Conecta a personas, locales y organizadores en una sola plataforma con calendario, mapa, menús, reservas y alertas exclusivas.",
  },
  {
    text: "Nos proyectamos como el nuevo ecosistema digital del entretenimiento en Noruega, conectando a todos los actores de la escena —desde el público hasta discotecas y organizadores— en una experiencia integrada, dinámica y en constante evolución.",
  },
];
---

<section class="relative py-20 overflow-hidden bg-white">
  <div
    class="flex flex-col items-center gap-12 px-6 mx-auto max-w-7xl lg:flex-row"
  >
    <!-- Texto -->
    <div class="lg:w-1/2">
      <h2 class="mb-8 text-4xl font-black text-zinc-900">¿QUÉ ES FESTIVA?</h2>

      <ul class="flex flex-col gap-6">
        {
          items.map((item) => (
            <li class="flex items-start gap-4">
              <div class="flex items-center justify-center p-2 text-sm text-white bg-pink-500 rounded-full">
                🎉
              </div>
              <p class="text-base leading-relaxed text-zinc-700">{item.text}</p>
            </li>
          ))
        }
      </ul>
    </div>

    <!-- Imagen -->
    <div class="relative lg:w-1/2">
      <img
        src="/images/club1.png"
        alt="¿Qué es Festiva?"
        class="shadow-xl rounded-2xl"
      />
      <!-- Decorativo debajo (línea tipo progreso) -->
      <div class="w-full h-2 mt-4 overflow-hidden bg-gray-200 rounded-full">
        <div class="w-1/3 h-full bg-pink-500 animate-pulse"></div>
      </div>
    </div>
  </div>
</section>
