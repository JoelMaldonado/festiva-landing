---
const { title, description, icon, color } = Astro.props;
---

<div
  class="bg-zinc-900 rounded-2xl p-5 shadow-xl transition hover:scale-[1.02] hover:shadow-yellow-400/10 text-white relative"
>
  <img src={icon} alt={title} class="mb-2 text-3xl" />
  <h4 class="mb-1 text-lg font-bold">{title}</h4>
  <p class="text-sm text-gray-300">{description}</p>
</div>
