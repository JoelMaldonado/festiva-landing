---
import InputField from "./ui/InputField.astro";
---

<div class="relative min-h-screen overflow-hidden">
  <!-- Círculos decorativos -->
  <img
    src="/images/circle_green.png"
    alt="Decoración"
    class="absolute top-0 right-0 z-0 w-40"
  />
  <img
    src="/images/circle_orange.png"
    alt="Decoración"
    class="absolute bottom-0 left-0 z-0 w-40"
  />

  <section id="contact" class="relative z-10 max-w-6xl px-6 py-20 mx-auto">
    <div class="mb-12 text-center">
      <h1 class="text-3xl font-extrabold uppercase md:text-5xl">
        ¿Eres una discoteca o negocio?
        <span class="block mt-2 text-p1">Únete a Festiva</span>
      </h1>
      <p class="max-w-xl mx-auto mt-4 text-lg text-gray-300">
        Las discotecas y negocios más top de Oslo ya están en <span
          class="font-semibold text-p1">Festiva</span
        >. ¿Y la tuya?<br />
        Estás perdiendo visibilidad cada noche. Contáctanos ahora.<br />
        <span class="font-bold text-p1">¡Es ahora o nunca!</span>
      </p>
    </div>

    <!-- Formulario y mockup -->
    <div class="flex flex-col items-center justify-center gap-10 md:flex-row">
      <div class="bg-[#1c1c1e] p-8 rounded-3xl w-full max-w-md shadow-lg">
        <h2 class="mb-6 text-xl font-semibold">Formulario de Contacto</h2>
        <p class="mb-6 text-gray-400">
          Forma parte del nuevo ecosistema de entretenimiento nocturno.
        </p>
        <form class="space-y-4">
          <InputField
            label="Nombres Completos"
            name="name"
            placeholder="Ingresa tus nombres"
            value=""
          />
          <InputField
            label="Correo Electrónico"
            name="email"
            placeholder="Ingresa tu correo"
            value=""
          />
          <InputField
            label="Nombre del negocio"
            name="business"
            placeholder="Ingresa tu negocio"
            value=""
          />
          <InputField
            label="Tipo de local"
            name="local"
            placeholder="Ingresa el tipo de local"
            value=""
          />
          <button
            type="submit"
            class="w-full py-3 font-semibold text-white bg-pink-500 rounded-lg hover:bg-pink-600"
          >
            Solicitar Información
          </button>
        </form>
      </div>

      <!-- Imagen del celular (mockup) -->
      <div>
        <img
          src="/images/mockup1.png"
          alt="Mockup app Festiva"
          class="max-w-[300px] rounded-xl"
        />
      </div>
    </div>
  </section>

  <!-- Cinta inferior -->
  <div
    class="py-3 text-lg font-extrabold tracking-widest text-center text-white uppercase bg-pink-500"
  >
    <marquee behavior="scroll" direction="left">
      REGÍSTRATE ● REGÍSTRATE ● REGÍSTRATE ● REGÍSTRATE ● REGÍSTRATE ●
      REGÍSTRATE
    </marquee>
  </div>
</div>
